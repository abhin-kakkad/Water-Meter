<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.application.watermeter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".admin_login" />
        <activity android:name=".user_login" />
        <activity android:name=".admin_logged_in" />
        <activity android:name=".admin_signup" />
        <activity android:name=".user_signup" />
        <activity android:name=".add_user_data" />
        <activity android:name=".user_logged_in" />
        <activity android:name=".user_change_password" />
        <activity android:name=".EnterSocietyDetails"></activity>
    </application>

</manifest>